{% extends 'base.html.twig' %}

{%  block header %}
    <h1>Here is your list...</h1>
{%  endblock %}

    {% block content %}
        {% for task in tasks %}
            <div class="panel panel-default">
                <div class="panel-body">
                    {{ task.name }}</a><br/>
                </div>
            </div>
        {% else %}
            No tasks
        {% endfor %}

    {% endblock %}

